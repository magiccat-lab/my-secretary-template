fastapi>=0.110
uvicorn>=0.29
requests>=2.31
python-dotenv>=1.0
PyYAML>=6.0
google-api-python-client>=2.120
google-auth>=2.29
google-auth-httplib2>=0.2
google-auth-oauthlib>=1.2

# Phase 1 拡張で追加
beautifulsoup4>=4.12          # recommendations sample [HTML scraping]
# discord.py>=2.3             # corpus_writer / channel_admin で realtime hook を有効化する場合のみ
#                               cron / batch path だけ使うなら不要、 default は commented
